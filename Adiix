--============================================--
-- Adiiiiiiix script v6
-- SPEED MODES + GUARANTEED GRAB
--============================================--

local player = game.Players.LocalPlayer
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local coins = player:WaitForChild("leaderstats"):WaitForChild("Coins")

local magnetOn, busy = false, false
local stolenTotal = 0
local watchTarget = nil
local lang = "EN"

--===== إعدادات السرعة =====--
local speedMode = 1 -- 1=SAFE, 2=FAST, 3=ULTRA
local SPEEDS = {
    [1] = {key = "speedSafe", tpWait = 0.15, pressWait = 0.20, maxTries = 4},
    [2] = {key = "speedFast", tpWait = 0.08, pressWait = 0.10, maxTries = 6},
    [3] = {key = "speedUltra", tpWait = 0.02, pressWait = 0.04, maxTries = 10},
}

local httpReq = (syn and syn.request) or (http and http.request) or http_request or request

--===== قاموس الترجمة =====--
local T = {
    EN = {
        title = "Adiiiiiiix script",
        tabTools = "TOOLS", tabServers = "SERVERS", tabPlayers = "PLAYERS",
        magnetName = "COIN MAGNET v2",
        magnetDesc = "Teleport snatch: grabs dropped cash and returns to your spot",
        refresh = "REFRESH SERVER LIST",
        loading = "LOADING...",
        noHttp = "HTTP NOT SUPPORTED",
        failed = "FAILED - TRY AGAIN",
        found = " found)",
        you = "[YOU] ",
        playersWord = " players",
        ping = "Ping: ",
        join = "JOIN",
        watch = "WATCH", stop = "STOP",
        watching = "Watching: ",
        stolen = "Stolen: ", dh = " DH",
        speed = "SPEED: ",
        speedSafe = "SAFE", speedFast = "FAST", speedUltra = "ULTRA",
    },
    AR = {
        title = "سكريبت Adiiiiiiix",
        tabTools = "أدوات", tabServers = "سيرفرات", tabPlayers = "لاعبين",
        magnetName = "مغناطيس الكوينز v2",
        magnetDesc = "ينتقل للكاش المرمي، يأخذه بسرعة، ويرجعك لمكانك",
        refresh = "تحديث قائمة السيرفرات",
        loading = "جاري التحميل...",
        noHttp = "HTTP غير مدعوم",
        failed = "فشل - حاول مجدداً",
        found = " سيرفر)",
        you = "[أنت] ",
        playersWord = " لاعب",
        ping = "البينغ: ",
        join = "دخول",
        watch = "مراقبة", stop = "إيقاف",
        watching = "تراقب: ",
        stolen = "المسروق: ", dh = " درهم",
        speed = "السرعة: ",
        speedSafe = "آمنة", speedFast = "سريعة", speedUltra = "خارقة",
    }
}

local function txt(key) return T[lang][key] end

--===== الواجهة =====--
local gui = Instance.new("ScreenGui")
gui.Name = "AdiiiiiiixScript"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

--== الأيقونة الدائرية ==--
local openBtn = Instance.new("TextButton")
openBtn.Size = UDim2.new(0, 52, 0, 52)
openBtn.Position = UDim2.new(0, 12, 0.5, -26)
openBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
openBtn.TextColor3 = Color3.fromRGB(255, 200, 0)
openBtn.Font = Enum.Font.GothamBold
openBtn.TextSize = 26
openBtn.Text = "A"
openBtn.Active = true
openBtn.Draggable = true
openBtn.Parent = gui
Instance.new("UICorner", openBtn).CornerRadius = UDim.new(1, 0)
local openGrad = Instance.new("UIGradient", openBtn)
openGrad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(60, 55, 30)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 20, 30))
}
openGrad.Rotation = 45
local openStroke = Instance.new("UIStroke", openBtn)
openStroke.Color = Color3.fromRGB(255, 200, 0)
openStroke.Thickness = 2

--== اللوحة الرئيسية ==--
local panel = Instance.new("Frame")
panel.Size = UDim2.new(0, 300, 0, 400)
panel.Position = UDim2.new(0, 80, 0.5, -200)
panel.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
panel.BorderSizePixel = 0
panel.Visible = false
panel.Active = true
panel.Draggable = true
panel.ClipsDescendants = true
panel.Parent = gui
Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 14)
local panelStroke = Instance.new("UIStroke", panel)
panelStroke.Color = Color3.fromRGB(255, 200, 0)
panelStroke.Thickness = 1.5
panelStroke.Transparency = 0.3

-- شريط العنوان
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 44)
topBar.BackgroundColor3 = Color3.fromRGB(28, 28, 40)
topBar.BorderSizePixel = 0
topBar.Parent = panel
Instance.new("UICorner", topBar).CornerRadius = UDim.new(0, 14)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -100, 1, 0)
title.Position = UDim2.new(0, 14, 0, 0)
title.BackgroundTransparency = 1
title.TextColor3 = Color3.fromRGB(255, 200, 0)
title.Font = Enum.Font.GothamBold
title.TextSize = 17
title.TextXAlignment = Enum.TextXAlignment.Left
title.Text = txt("title")
title.Parent = topBar

local langBtn = Instance.new("TextButton")
langBtn.Size = UDim2.new(0, 44, 0, 30)
langBtn.Position = UDim2.new(1, -88, 0, 7)
langBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
langBtn.TextColor3 = Color3.fromRGB(255, 200, 0)
langBtn.Font = Enum.Font.GothamBold
langBtn.TextSize = 13
langBtn.Text = "AR"
langBtn.Parent = topBar
Instance.new("UICorner", langBtn).CornerRadius = UDim.new(0, 8)
local langStroke = Instance.new("UIStroke", langBtn)
langStroke.Color = Color3.fromRGB(255, 200, 0)
langStroke.Thickness = 1

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -38, 0, 7)
closeBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 14
closeBtn.Text = "X"
closeBtn.Parent = topBar
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(1, 0)
closeBtn.MouseButton1Click:Connect(function() panel.Visible = false end)
openBtn.MouseButton1Click:Connect(function() panel.Visible = not panel.Visible end)

--== شريط التبويبات ==--
local tabBar = Instance.new("Frame")
tabBar.Size = UDim2.new(1, -16, 0, 34)
tabBar.Position = UDim2.new(0, 8, 0, 52)
tabBar.BackgroundTransparency = 1
tabBar.Parent = panel

local tabLayout = Instance.new("UIListLayout", tabBar)
tabLayout.FillDirection = Enum.FillDirection.Horizontal
tabLayout.Padding = UDim.new(0, 6)
tabLayout.SortOrder = Enum.SortOrder.LayoutOrder

local tabs, pages = {}, {}

local function makeTab(tabKey, order)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0, 88, 1, 0)
    b.BackgroundColor3 = Color3.fromRGB(35, 35, 50)
    b.TextColor3 = Color3.fromRGB(160, 160, 170)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 12
    b.Text = txt(tabKey)
    b.LayoutOrder = order
    b.Parent = tabBar
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)

    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1, -16, 1, -150)
    page.Position = UDim2.new(0, 8, 0, 94)
    page.BackgroundTransparency = 1
    page.ScrollBarThickness = 4
    page.ScrollBarImageColor3 = Color3.fromRGB(255, 200, 0)
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = false
    page.Parent = panel

    local pl = Instance.new("UIListLayout", page)
    pl.Padding = UDim.new(0, 6)
    pl.SortOrder = Enum.SortOrder.LayoutOrder

    tabs[tabKey] = b
    pages[tabKey] = page

    b.MouseButton1Click:Connect(function()
        for n, btn in pairs(tabs) do
            local active = (n == tabKey)
            btn.BackgroundColor3 = active and Color3.fromRGB(255, 200, 0) or Color3.fromRGB(35, 35, 50)
            btn.TextColor3 = active and Color3.fromRGB(20, 20, 30) or Color3.fromRGB(160, 160, 170)
            pages[n].Visible = active
        end
    end)
    return page
end

local toolsPage = makeTab("tabTools", 1)
local serversPage = makeTab("tabServers", 2)
local playersPage = makeTab("tabPlayers", 3)

-- شريط الحالة
local statusBar = Instance.new("TextLabel")
statusBar.Size = UDim2.new(1, -16, 0, 44)
statusBar.Position = UDim2.new(0, 8, 1, -52)
statusBar.BackgroundColor3 = Color3.fromRGB(14, 14, 20)
statusBar.TextColor3 = Color3.fromRGB(255, 200, 0)
statusBar.Font = Enum.Font.GothamBold
statusBar.TextSize = 12
statusBar.Parent = panel
Instance.new("UICorner", statusBar).CornerRadius = UDim.new(0, 8)

local function updateStatus()
    statusBar.Text = txt("stolen") .. stolenTotal .. txt("dh")
end
updateStatus()

tabs["tabTools"].BackgroundColor3 = Color3.fromRGB(255, 200, 0)
tabs["tabTools"].TextColor3 = Color3.fromRGB(20, 20, 30)
pages["tabTools"].Visible = true

--============================================--
-- TAB 1: TOOLS (بطاقة أكبر فيها زر سرعة)
--============================================--
local toolCards = {}

local magnetCard = Instance.new("Frame")
magnetCard.Size = UDim2.new(1, -4, 0, 100)
magnetCard.BackgroundColor3 = Color3.fromRGB(32, 32, 46)
magnetCard.BorderSizePixel = 0
magnetCard.LayoutOrder = 1
magnetCard.Parent = toolsPage
Instance.new("UICorner", magnetCard).CornerRadius = UDim.new(0, 10)

local magnetName = Instance.new("TextLabel")
magnetName.Size = UDim2.new(1, -70, 0, 24)
magnetName.Position = UDim2.new(0, 12, 0, 8)
magnetName.BackgroundTransparency = 1
magnetName.TextColor3 = Color3.new(1, 1, 1)
magnetName.Font = Enum.Font.GothamBold
magnetName.TextSize = 14
magnetName.TextXAlignment = Enum.TextXAlignment.Left
magnetName.Text = txt("magnetName")
magnetName.Parent = magnetCard

local magnetDesc = Instance.new("TextLabel")
magnetDesc.Size = UDim2.new(1, -70, 0, 34)
magnetDesc.Position = UDim2.new(0, 12, 0, 30)
magnetDesc.BackgroundTransparency = 1
magnetDesc.TextColor3 = Color3.fromRGB(150, 150, 160)
magnetDesc.Font = Enum.Font.Gotham
magnetDesc.TextSize = 11
magnetDesc.TextWrapped = true
magnetDesc.TextXAlignment = Enum.TextXAlignment.Left
magnetDesc.TextYAlignment = Enum.TextYAlignment.Top
magnetDesc.Text = txt("magnetDesc")
magnetDesc.Parent = magnetCard

-- زر التشغيل
local magnetToggle = Instance.new("TextButton")
magnetToggle.Size = UDim2.new(0, 52, 0, 26)
magnetToggle.Position = UDim2.new(1, -62, 0, 10)
magnetToggle.BackgroundColor3 = Color3.fromRGB(120, 120, 120)
magnetToggle.TextColor3 = Color3.new(1, 1, 1)
magnetToggle.Font = Enum.Font.GothamBold
magnetToggle.TextSize = 12
magnetToggle.Text = "OFF"
magnetToggle.Parent = magnetCard
Instance.new("UICorner", magnetToggle).CornerRadius = UDim.new(1, 0)

-- زر السرعة (يتنقل: SAFE -> FAST -> ULTRA)
local speedBtn = Instance.new("TextButton")
speedBtn.Size = UDim2.new(1, -24, 0, 26)
speedBtn.Position = UDim2.new(0, 12, 0, 66)
speedBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 220)
speedBtn.TextColor3 = Color3.new(1, 1, 1)
speedBtn.Font = Enum.Font.GothamBold
speedBtn.TextSize = 12
speedBtn.Parent = magnetCard
Instance.new("UICorner", speedBtn).CornerRadius = UDim.new(0, 8)

local SPEED_COLORS = {
    [1] = Color3.fromRGB(0, 140, 220),   -- SAFE أزرق
    [2] = Color3.fromRGB(230, 150, 0),   -- FAST برتقالي
    [3] = Color3.fromRGB(220, 40, 40),   -- ULTRA أحمر
}

local function refreshSpeedBtn()
    speedBtn.Text = txt("speed") .. txt(SPEEDS[speedMode].key)
    speedBtn.BackgroundColor3 = SPEED_COLORS[speedMode]
end
refreshSpeedBtn()

speedBtn.MouseButton1Click:Connect(function()
    speedMode = speedMode % 3 + 1
    refreshSpeedBtn()
end)

magnetToggle.MouseButton1Click:Connect(function()
    magnetOn = not magnetOn
    magnetToggle.Text = magnetOn and "ON" or "OFF"
    magnetToggle.BackgroundColor3 = magnetOn and Color3.fromRGB(0, 170, 90)
        or Color3.fromRGB(120, 120, 120)
end)

--============================================--
-- محرك COIN MAGNET: حمل مضمون 100%
-- لا يرجع أبداً قبل أن يتأكد أن الكاش دخل جيبك
--============================================--
local function snatch(cashObj)
    if busy or not magnetOn then return end
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local part = cashObj:IsA("BasePart") and cashObj
        or cashObj:FindFirstChildWhichIsA("BasePart", true)
    if not part then return end

    busy = true
    local spd = SPEEDS[speedMode]
    local returnPos = hrp.CFrame
    local before = coins.Value
    local grabbed = false

    -- 1) انتقال فوق الكاش
    hrp.CFrame = part.CFrame + Vector3.new(0, 3, 0)
    task.wait(spd.tpWait)

    -- 2) حلقة الضغط المضمون: يضغط ويتحقق، ويكرر حتى يدخل المال
    for i = 1, spd.maxTries do
        -- لو الكاش انحذف = شخص آخر أخذه أو نجحنا
        if not cashObj.Parent then grabbed = (coins.Value > before) break end

        local prompt = cashObj:FindFirstChildOfClass("ProximityPrompt", true)
        if prompt then
            -- نصفر زمن الضغطة حتى لا يفشل في السرعات العالية
            pcall(function()
                prompt.HoldDuration = 0
                prompt.MaxActivationDistance = math.max(prompt.MaxActivationDistance, 15)
            end)
            pcall(function() fireproximityprompt(prompt) end)
        end
        task.wait(spd.pressWait)

        -- التحقق الحقيقي: هل دخل المال؟
        if coins.Value > before then
            grabbed = true
            break
        end
        -- إعادة تثبيت الموقع فوق الكاش (لو اهتز)
        if hrp.Parent and part.Parent then
            hrp.CFrame = part.CFrame + Vector3.new(0, 3, 0)
        end
    end

    -- 3) العودة فقط بعد التأكد (أو استنفاد المحاولات)
    if hrp.Parent then hrp.CFrame = returnPos end

    if grabbed then
        local amount = coins.Value - before
        stolenTotal += amount
        updateStatus()
        warn("[Adiiiiiiix] Snatched +" .. amount .. " DH! [" .. txt(SPEEDS[speedMode].key) .. "]")
    else
        warn("[Adiiiiiiix] Grab failed after " .. spd.maxTries .. " tries (someone took it?)")
    end
    busy = false
end

workspace.ChildAdded:Connect(function(obj)
    if obj.Name:lower():find("cash") then
        task.wait(0.05)
        pcall(snatch, obj)
    end
end)
task.spawn(function()
    while gui.Parent do
        if magnetOn and not busy then
            for _, obj in ipairs(workspace:GetChildren()) do
                if obj.Name:lower():find("cash") then pcall(snatch, obj) end
            end
        end
        task.wait(0.4)
    end
end)

--============================================--
-- TAB 2: SERVERS
--============================================--
local refreshBtn = Instance.new("TextButton")
refreshBtn.Size = UDim2.new(1, -4, 0, 34)
refreshBtn.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
refreshBtn.TextColor3 = Color3.fromRGB(20, 20, 30)
refreshBtn.Font = Enum.Font.GothamBold
refreshBtn.TextSize = 13
refreshBtn.Text = txt("refresh")
refreshBtn.LayoutOrder = 0
refreshBtn.Parent = serversPage
Instance.new("UICorner", refreshBtn).CornerRadius = UDim.new(0, 8)

local lastServers = {}

local function clearList(page)
    for _, c in ipairs(page:GetChildren()) do
        if c:IsA("GuiObject") and c.LayoutOrder > 0 then c:Destroy() end
    end
end

local function renderServers(list)
    clearList(serversPage)
    local order = 1
    for _, srv in ipairs(list) do
        local isMe = (srv.id == game.JobId)
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, -4, 0, 52)
        row.BackgroundColor3 = isMe and Color3.fromRGB(70, 55, 15) or Color3.fromRGB(32, 32, 46)
        row.BorderSizePixel = 0
        row.LayoutOrder = order
        row.Parent = serversPage
        Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)
        if isMe then
            local st = Instance.new("UIStroke", row)
            st.Color = Color3.fromRGB(255, 200, 0)
            st.Thickness = 2
        end

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, -70, 1, -8)
        lbl.Position = UDim2.new(0, 12, 0, 4)
        lbl.BackgroundTransparency = 1
        lbl.TextColor3 = Color3.new(1, 1, 1)
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 12
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextYAlignment = Enum.TextYAlignment.Center
        lbl.Text = (isMe and txt("you") or "") .. string.sub(srv.id, 1, 8) ..
            "  |  " .. tostring(srv.playing) .. "/" .. tostring(srv.maxPlayers) ..
            txt("playersWord") .. "\n" .. txt("ping") .. tostring(srv.ping or "?") .. " ms"
        lbl.Parent = row

        if not isMe then
            local joinBtn = Instance.new("TextButton")
            joinBtn.Size = UDim2.new(0, 52, 0, 28)
            joinBtn.Position = UDim2.new(1, -60, 0.5, -14)
            joinBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 220)
            joinBtn.TextColor3 = Color3.new(1, 1, 1)
            joinBtn.Font = Enum.Font.GothamBold
            joinBtn.TextSize = 12
            joinBtn.Text = txt("join")
            joinBtn.Parent = row
            Instance.new("UICorner", joinBtn).CornerRadius = UDim.new(0, 8)
            local jobId = srv.id
            joinBtn.MouseButton1Click:Connect(function()
                joinBtn.Text = "..."
                pcall(function()
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, jobId, player)
                end)
                task.wait(3)
                joinBtn.Text = txt("join")
            end)
        end
        order = order + 1
    end
    refreshBtn.Text = txt("refresh") .. " (" .. tostring(order - 1) .. txt("found")
end

local function loadServers()
    refreshBtn.Text = txt("loading")
    task.spawn(function()
        if not httpReq then
            refreshBtn.Text = txt("noHttp")
            task.wait(2)
            refreshBtn.Text = txt("refresh")
            return
        end
        local ok, res = pcall(function()
            return httpReq({
                Url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100",
                Method = "GET"
            })
        end)
        if not ok or not res or not res.Body then
            refreshBtn.Text = txt("failed")
            task.wait(2)
            refreshBtn.Text = txt("refresh")
            return
        end
        local data = HttpService:JSONDecode(res.Body)
        lastServers = data.data or {}
        renderServers(lastServers)
    end)
end

refreshBtn.MouseButton1Click:Connect(loadServers)

--============================================--
-- TAB 3: PLAYERS
--============================================--
local camera = workspace.CurrentCamera

local function stopWatch()
    watchTarget = nil
    if player.Character then
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum then camera.CameraSubject = hum end
    end
end

local function buildPlayerList()
    clearList(playersPage)
    local order = 1
    for _, pl in ipairs(Players:GetPlayers()) do
        if pl ~= player then
            local row = Instance.new("Frame")
            row.Size = UDim2.new(1, -4, 0, 44)
            row.BackgroundColor3 = Color3.fromRGB(32, 32, 46)
            row.BorderSizePixel = 0
            row.LayoutOrder = order
            row.Parent = playersPage
            Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)

            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(1, -80, 1, 0)
            lbl.Position = UDim2.new(0, 12, 0, 0)
            lbl.BackgroundTransparency = 1
            lbl.TextColor3 = Color3.new(1, 1, 1)
            lbl.Font = Enum.Font.GothamBold
            lbl.TextSize = 13
            lbl.TextXAlignment = Enum.TextXAlignment.Left
            lbl.Text = pl.DisplayName .. " (@" .. pl.Name .. ")"
            lbl.Parent = row

            local watchBtn = Instance.new("TextButton")
            watchBtn.Size = UDim2.new(0, 60, 0, 28)
            watchBtn.Position = UDim2.new(1, -68, 0.5, -14)
            watchBtn.BackgroundColor3 = Color3.fromRGB(150, 60, 180)
            watchBtn.TextColor3 = Color3.new(1, 1, 1)
            watchBtn.Font = Enum.Font.GothamBold
            watchBtn.TextSize = 11
            watchBtn.Text = txt("watch")
            watchBtn.Parent = row
            Instance.new("UICorner", watchBtn).CornerRadius = UDim.new(0, 8)

            local targetPl = pl
            watchBtn.MouseButton1Click:Connect(function()
                if watchTarget == targetPl then
                    stopWatch()
                    watchBtn.Text = txt("watch")
                    watchBtn.BackgroundColor3 = Color3.fromRGB(150, 60, 180)
                    updateStatus()
                else
                    local char = targetPl.Character
                    local hum = char and char:FindFirstChildOfClass("Humanoid")
                    if hum then
                        watchTarget = targetPl
                        camera.CameraSubject = hum
                        watchBtn.Text = txt("stop")
                        watchBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
                        statusBar.Text = txt("watching") .. targetPl.DisplayName
                    end
                end
            end)
            order = order + 1
        end
    end
end

Players.PlayerAdded:Connect(buildPlayerList)
Players.PlayerRemoving:Connect(function(pl)
    if watchTarget == pl then stopWatch() end
    buildPlayerList()
end)
task.spawn(function()
    while gui.Parent do
        if watchTarget then
            local char = watchTarget.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum and camera.CameraSubject ~= hum then
                camera.CameraSubject = hum
            end
        end
        task.wait(1)
    end
end)

tabs["tabPlayers"].MouseButton1Click:Connect(buildPlayerList)
buildPlayerList()

--============================================--
-- زر تبديل اللغة 🌐
--============================================--
langBtn.MouseButton1Click:Connect(function()
    lang = (lang == "EN") and "AR" or "EN"
    langBtn.Text = (lang == "EN") and "AR" or "EN"
    title.Text = txt("title")
    tabs["tabTools"].Text = txt("tabTools")
    tabs["tabServers"].Text = txt("tabServers")
    tabs["tabPlayers"].Text = txt("tabPlayers")
    magnetName.Text = txt("magnetName")
    magnetDesc.Text = txt("magnetDesc")
    refreshSpeedBtn()
    updateStatus()
    refreshBtn.Text = txt("refresh")
    if #lastServers > 0 then renderServers(lastServers) end
    buildPlayerList()
end)

print("[Adiiiiiiix script v6] Ready! Speed modes + guaranteed grab")
